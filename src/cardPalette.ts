// Shared colors for the bespoke card family (TitleCardBody, CardBody) --
// a one-off design brief's own palette, separate from theme.ts's line/bar
// chart palette system (CLAUDE.md Section 13). Factored out once a second
// consumer (CardBody) needed the same corner-mark/bottom-bar colors, so both
// stay in sync if this palette ever changes.
export const BG = "#191510";
export const ACCENT = "#ff6b35";
export const EYEBROW_TEXT = "#a29583";
export const HEADLINE_TEXT = "#f7f3ea";
export const BUTTER_YELLOW = "#f5d472";
